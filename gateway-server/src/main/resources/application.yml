server:
  port: 8080

spring:
  application:
    name: gateway-server
  config:
    import: optional:configserver:http://configserver:8071
  cloud:
    config:
      fail-fast: true
      retry:
        initial-interval: 2000
        max-interval: 10000
        max-attempts: 100